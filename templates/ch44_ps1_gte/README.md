# ch44_ps1_gte — Geometry Transformation Engine

Exercises:

| Dir               | Topic                                                   |
|-------------------|---------------------------------------------------------|
| `01_cop2_regs`    | COP2 register spaces, command decode, saturation/FLAG   |
| `02_rtps`         | RTPS/RTPT: wide math, perspective divide, SXY FIFO      |
| `03_lighting`     | NCDS lighting pipeline, MVMVA, AVSZ3                    |
| `90_debug`        | seeded bugs: LM-ignoring clamp + silent divide overflow |
| `91_challenge`    | conformance harness over register-snapshot files         |
| `99_coding_test`  | unseen-spec opcode NCLIP (backface culling)             |

Run:

```bash
LABS=ch44_ps1_gte make skels && make build && ctest --test-dir build -R ch44
python3 tools/labs/generate.py --mode solution --force --targets ch44_ps1_gte
cmake -S . -B build-solutions -DLABS_BUILD_SOLUTIONS=On && \
  cmake --build build-solutions -j && ctest --test-dir build-solutions -R ch44
```

Gate checklist:

- [x] exercises: skel RED -> student GREEN
- [x] starter: skeleton tree configures and builds
- [x] debug: `90_debug` + `bug-report.md`
- [x] challenge: `91_challenge` conformance goldens reproduced
- [x] coding test: hidden manifest passes (NCLIP edge cases)

## Verification

```
VERIFY_PREFIX=/tmp/labs-PS1B tools/labs/verify_chapter.sh ch44_ps1_gte ...
[verify] verdict: skel_build=ok solutions=GREEN
```

Conformance goldens under `tests/public|hidden/ch44_ps1_gte/conf/` are
generated by the reference implementation (run twice, byte-identical);
see `tests/public/ch44_ps1_gte/provenance.md`.
