# Provenance — ch22 hidden fixtures & goldens

All goldens generated by the reference solution, run twice, identical.

- `fixtures/scroll_scene.nesf` — ch21 scene_grid scene (same NESF v1
  format); frame golden `69F4AFF694879225` from
  `ch22_03_sprites_runner --rom fixtures/scroll_scene.nesf --frames 1`.
- `fixtures/pair_{h,v,o}_{a,b}.rgba` — produced by `tools/gen_pairs.py`
  from the reference render of that scene; classifications verified with
  the reference `ch22_99_frame_diff` (h1 / v1 / other).
- Challenge timing goldens (`2BDC4EA0E4CB3D2C`, `177E961AC7918A29`) from
  `ch22_91_timing_runner` over the committed scripts in the chapter
  template's `91_challenge/fixtures/`.
