# Provenance — ch23 hidden fixtures & goldens

Produced by `tools/gen_hidden.py` (course-original, deterministic pattern
bytes). Goldens generated by the reference solution, run twice, identical.

- `fixtures/unseen_header.nes` — mapper 0x63 (both iNES nibble sources),
  trainer, vertical mirroring. Runner trace hash (FNV64 of the summary
  line): `574D85EA90290DCD`.
- `fixtures/unseen_mfx.nes` + `unseen_mfx_script.txt` — unseen MFX-1 cart
  (8 PRG banks, CHR-RAM board) and op script exercising PRG modes A/B,
  PRG RAM, echo-CHR fallback and the one-shot timer. Log hash:
  `36D6EC6C9AA99755`.

All eight manifest cases were executed against scratch-built reference
solution binaries: 8/8 pass.
