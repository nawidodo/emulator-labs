# Provenance — ch24 public fixtures & goldens

Generated by the reference solution tree (ch24_nes_apu_dma_sync), run
twice, byte-identical output both times.

## Fixture

`fixtures/drift_probe.txt` — course-original op script (patterned tiles,
pulse+triangle melody with length loads, frame-IRQ enable, mid-run scroll
change). Committed verbatim; nothing generated.

## Golden hashes (FNV-1a 64)

Command (reference solution runner):

    ch24_04_sync_runner --rom tests/public/ch24_nes_apu_dma_sync/fixtures/drift_probe.txt \
        --frames 40 --trace /tmp/d.txt --hash-frame /tmp/d.rgba --audio-out /tmp/d.pcm
    python3 tools/labs/hash_frame.py /tmp/<file> --fnv-only

| Output | FNV64 |
|---|---|
| trace (`--trace`)   | DF8EBD263D84E436 |
| last frame RGBA (`--hash-frame`) | 51F3538840465A25 |
| audio PCM (`--audio-out`)        | 2C49BA3A3585344D |

With the broken ratio (2 dots per CPU cycle) the audio sample count per
frame and the cycle columns in the trace both diverge, so every hash here
misses — that is the drift-repair contract.
