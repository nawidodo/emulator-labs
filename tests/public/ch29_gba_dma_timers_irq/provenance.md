# Provenance — ch29 public fixtures & traces

Synthetic `.hws` scripts (records `{u32 cycle, u32 addr, u16 value}`,
terminator cycle `0xFFFFFFFF`). No ROM data of any kind.

- `fixtures/race_fixture.hws`: arms TM0 (period 5 ticks, IRQ on) and issues
  IE/IME writes colliding at cycle 10 to pin insertion-order tie-breaking.
  Golden trace `traces/race_golden.log` produced by the reference solution
  runner (`ch29_hw_runner --cycles 60 --trace`); run twice, byte-identical.
  Final state digest: 20D6AD6F856AFED2.
- `fixtures/hb_timer_mix.hws`: HBlank-triggered repeating DMA1 plus a
  prescaled timer. Golden `traces/hb_timer_mix_golden.log` via
  `--cycles 4000 --trace`; run twice, byte-identical.

All outputs generated by the solution tree built by
tools/labs/verify_chapter.sh (isolated prefix).
