# Provenance — ch30 public fixtures & audio goldens

Synthetic fixtures only; no ROM data.

- `fixtures/dsound_ramp.hws`: `.hws` script (format: `{u32 cycle, u32 addr,
  u16 value}` records, terminator cycle `0xFFFFFFFF`) pushing two 32-byte
  ramps into Direct Sound FIFO A (0x040000A0) and FIFO B (0x040000A4).
- Golden digest for the hidden manifest case `audio_hash_golden` produced
  by the reference-solution runner:
  `ch30_audio_runner --rom dsound_ramp.hws --samples 128 --hash-frame`
  -> FNV-64 of the digest file = **2EE89BA755992E6D**. Run twice,
  byte-identical.
- Challenge scene golden (PSG + Direct Sound mixed render) embedded as
  `2550657C798F36FD` in `templates/ch30_gba_audio_saves_accuracy/91_challenge/
  main.cpp`; generated by running that test twice against the solution tree.

No randomness, no wall-clock inputs anywhere in the pipeline.
