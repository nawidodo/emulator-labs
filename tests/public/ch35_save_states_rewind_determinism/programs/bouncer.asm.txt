# bouncer.bin — synthetic CHIP-8 program (hand-assembled)

Author: ch35 authoring, 2026-08. Not derived from any commercial ROM or
the Timendus suite (which is referenced by URL only, optional).

```
addr  bytes     insn            ; meaning
200   A2 30     LD   I,$230     ; sprite at file offset 0x30
202   60 00     LD   V0,$00     ; x = 0
204   61 00     LD   V1,$00     ; y = 0
206   62 3F     LD   V2,$3F     ; x mask (wrap over 64 px)
208   63 1F     LD   V3,$1F     ; y mask (wrap over 32 px)
20A   D0 15     DRW  V0,V1,5    ; XOR-draw 5-row sprite
20C   70 03     ADD  V0,$03     ; x += 3
20E   71 02     ADD  V1,$02     ; y += 2
210   80 12     AND  V0,V2      ; wrap x
212   81 32     AND  V1,V3      ; wrap y
214   12 0A     JP   $20A       ; loop

230   AA 55 AA 55 AA            ; dither sprite
```

Behavior: an XOR-animated diagonal pattern that wraps forever — never
terminates, exercises DRW/VX/RNG-free paths deterministically. Used for
save/rewind goldens. Frame model: 10 instructions + one DT/ST tick per
frame.
